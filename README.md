# MongoDB Data Model for NPM package `session-calling-menu`

----------

```
let model = require('session-calling-menu-model-mongo')(
    "mongodb://127.0.0.1/mongoUrl",
    "collectionName",{
        expireTime: 3600*1000
    });
```
